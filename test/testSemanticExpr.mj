// Test301

program test301

enum Broj { NULA, JEDAN, PET=5, DESET=10}

const int nula = 0;
const int jedan = 1;
const int pet = 5;

int niz[];
char nizch[];

int a;
bool b;
int c;
	
{
	int sum(int a, int b){
	return a + b;
	}
	
	void main()	
		int bodovi;
		int temp;
		Broj nula, jedan;
		bool bt;
	{
		nula = Broj.NULA;
		jedan = Broj.JEDAN;
		bodovi = 0;
		bodovi++;
		bodovi = bodovi + jedan;
		bodovi = bodovi * pet;
		bodovi--;
		print(bodovi); //9
		print(eol);
		niz = new int[3];
		niz[nula] = 2;
		niz[jedan] = 3;
		niz[niz[nula]] = niz[1] * 4;
		bodovi = niz[2]/niz[0];
		print(bodovi); //6
		print(eol);
		print(niz[2]); //12
		print(eol);
		
		nizch = new char[3];
		nizch[0] = 'a';
		nizch[jedan] = 'b';
		nizch[pet - 3] = 'c';
		print(nizch[1]); //b
		print(eol);
		print(nizch[jedan * 2]); //c
		print(eol);
		print(ord(nizch[0])); //97

		print(nizch.length); // 3
		print(niz.length); // 3

		a=1;
		read(bodovi);
		bodovi = -bodovi + (pet * a / 2 - nula) * bodovi - (3 % 2 + 3 * 2 - 7); 
		print(bodovi); //bodovi
		
		read(bodovi);
		bt = true;
		
		print(bt ? bodovi : bodovi + 10);
		print(false ? bodovi + 20 : bodovi + 30);
		print(bodovi > 10 ? '+' : '-');
		
		
		b = NepostojeciEnum.prvi;
		a = Broj.prvi;
		a[5] = 6;
		niz[jedan] = 7;
		niz[bt] = 8;
		a = sum(a, 1);
		a = add(a, 1);
		a = new int[bt];
		a = bt * 5 / 6;
		a = 5 * bt / 6;
		a = 5 + bt - 6;
		if(-bt == 5) a = 1;
		else a = 2;
		print(bodovi + 10 ? '+' : '-');
		print(true ? '+' : 5);
		if(niz < niz) a = 5; 
		if(niz == niz && 5) a = 5;
		if(niz == niz || 5) a = 5;
		a = bt;
		sum = bt;
		Broj.JEDAN++;
		b++;
		sum(sum(sum(a, a, a), c), a);
		sum(a,b,c);
		a(a,b);
	}
}


